#ifndef SEQUENTIAL_HPP
#define SEQUENTIAL_HPP

/*
Procura uma chave num vetor usando uma busca sequencial modificada
recebe: o vector e a chave a ser pesquisada
retorna: a primeira posição onde o elemento está ou -1 se não for encontrado
*/
int search(std::vector<int>, int);

#endif