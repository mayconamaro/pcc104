#ifndef CLOSEST_PAIR
#define CLOSEST_PAIR

#include <vector>
#include <utility>

/*
Calcula a distância entre o par de pontos mais próximos
recebe: uma lista de pontos
retorna: a distância do par mais próximo
*/
double findShortest(std::vector<std::pair<int, int>>);

#endif